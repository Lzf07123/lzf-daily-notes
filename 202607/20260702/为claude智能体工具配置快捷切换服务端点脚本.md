# 为claude智能体工具配置快捷切换服务端点脚本

## Context

不同的大模型有着各自擅长的领域，每次更改环境变量过于繁琐，简单写了个快捷切换脚本

---

## Key Takeaway

- 运用`变量`知识点；使用变量存储大模型端点、apikey、使用模型
- 运用`函数`知识点；包装清除旧环境变量的重复unset操作，后续可直接复用函数
- 运用`条件分支`知识点；判断需要切换到模型/端点
- 运用`判断`知识点；判断传入参数是否合规

---

## Pratice

[apilist](../../resource/apilist.sh)
