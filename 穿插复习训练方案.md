# 省赛云计算赛项 — 穿插复习训练方案

> **制定日期：** 2026-07-26  
> **启动日期：** 2026-08-01（7/27-31 AI 集训，顺延 5 天）  
> **基于：** 2025-2026 学年广东省职业技能大赛云计算应用赛题（四模块）  
> **原则：** 按赛题顺序推进，学新不忘旧，复习降频不降质

---

## 一、整体架构

### 1.1 模块推进与复习衰减

```
模块一(5-6周)    模块二(6-8周)    模块三(4-5周)    模块四(3-4周)    真题拉练(4周)
OpenStack ────→ K8s/Docker ────→ 边缘计算 ────→ 大模型 ──────→ 模拟赛
  │ 学完后         │ 学完后          │ 学完后         │ 学完后
  │ 🔵活跃复习     │ 🟢维持复习      │               │
  └────────────→  │ 🔵活跃复习 ───→ │ 🟢维持复习     │
                   └──────────────→ │ 🔵活跃复习 ──→ │ 🟢维持复习
                                    └──────────────→ │ 🔵活跃复习 → 🟢维持
```

### 1.2 复习频率衰减规则

| 模块状态              | 复习频率       | 单次耗时  | 复习方式            |
| :-------------------- | :------------- | :-------- | :------------------ |
| 🔴 正在学             | 每天（主攻段） | 90-120min | 手搓 + 调优 + 排错  |
| 🔵 刚学完（≤1个月）   | 每 3 天        | 15-20min  | 计时部署 / 脚本一键 |
| 🟢 熟练期（1-3个月）  | 每 7 天        | 20-30min  | 联调 + 排错练习     |
| ⚪ 长期保持（>3个月） | 每 14 天       | 30-45min  | 全链路 + 混沌排错   |

### 1.3 每日三段式（150min = 2.5h）

```
┌──────────────────────────────────────────────────────────────┐
│ 🔥 热身段  15-20min   从复习池中轮转1个老考点                    │
│           · 计时部署（≤25min单组件）                            │
│           · 不看笔记                                            │
│           · 只跑关键步骤                                        │
├──────────────────────────────────────────────────────────────┤
│ 🚀 主攻段  90-120min  推进当前子阶段新内容                        │
│           · 手搓 → 理解 → iaas脚本 → 调优参数                     │
│           · 遇错先自己排查5min，再查笔记/文档                       │
├──────────────────────────────────────────────────────────────┤
│ 💡 收尾段  10-15min   对比新旧配置文件 + 一句话笔记                  │
│           · 新旧组件三段式对比（认证/数据库/特有）                    │
│           · 今天踩了什么坑 + 明天热身选谁                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、模块一：OpenStack 私有云（5-6周）

模块一赛题共 **22个考点**，拆为 6 个子阶段：

```
1A:存储收尾 ──→ 1B:界面编排 ──→ 1C:调优参数 ──→ 1D:监控工具 ──→ 1E:Python开发 ──→ 1F:整合拉练
    ~7天           ~7天            ~7天            ~5天            ~7天             ~4天
```

### 2.1 各子阶段考点清单

| 子阶段            | 新学内容                                                                                                  | 赛题对应                                        | 预计天数 |
| :---------------- | :-------------------------------------------------------------------------------------------------------- | :---------------------------------------------- | :------- |
| **1A 存储收尾**   | Cinder(LVM)+Swift(分段存放)+ISCSI(target+ACL)+Glance指定镜像上传                                          | 1.1.5, 1.1.9, 1.1.10, 1.2.7                     | 7天      |
| **1B 界面编排**   | Dashboard(部署+文件存储+标题)+Keystone多租户(域/项目/组/用户+角色)+Heat(YAML)+Cgroup(CPU限制20%)          | 1.1.4, 1.1.8, 1.2.1, 1.2.10                     | 7天      |
| **1C 调优参数**   | MariaDB(4参数)+Memcached(3参数)+Nova(缓存调度器+并发限制+白名单)+Glance(缓存+API超时)+Keystone(memcached) | 1.1.3, 1.1.6, 1.2.2, 1.2.3, 1.2.6, 1.2.8, 1.2.9 | 7天      |
| **1D 监控工具**   | Prometheus+Grafana裸机(systemd)+node_exporter监控网络带宽+qemu镜像压缩                                    | 1.2.4, 1.2.5                                    | 5天      |
| **1E Python开发** | MariaDB DM_Manager类(PyMySQL)+OpenStack CLI(FastAPI+argparse 端口9046)                                    | 1.3.1, 1.3.2                                    | 7天      |
| **1F 整合拉练**   | Shell一键脚本(Keystone→ISCSI)+iaas-yoga全流程计时+模块一模拟考                                            | —                                               | 4天      |

### 2.2 1A 存储收尾（7天·当前阶段）

**新内容：** Cinder LVM → Swift 对象存储 → ISCSI 存储

```
Day │ 热身段(15-20min)              │ 主攻段(90-120min)              │ 收尾段(10-15min)
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 计时部署 Keystone（≤25min）    │ Cinder LVM 手搓部署             │ Keystone vs Cinder 三段式对比
    │ 无笔记，全流程                 │ + iaas-install-cinder-*.sh     │ 认证段完全一样，数据库段也相同
    │                              │ 理解 LVM VG/LV/PV 关系          │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时部署 Glance（≤15min）      │ Cinder 挂盘/扩容/快照 全流程      │ Glance vs Cinder 三段式对比
    │ 🆕 上传 cirros-0.5.2 镜像       │ 创建云硬盘→挂载→扩容→快照→恢复    │ 特有段: glance_store vs LVM
    │ 命名为指定名称（赛题要求）        │                              │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 计时部署 Neutron（网桥检查）    │ Cinder iaas脚本计时练习           │ Neutron 网桥 vs Cinder LVM
    │ 确认 linuxbridge_agent.ini    │ 目标: 从脚本启动到卷创建 ≤15min    │ 网络配置 vs 存储配置
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 计时部署 Nova（配置段检查）     │ Swift 手搓部署                   │ Nova配置段 vs Swift存储节点
    │ controller+compute 都跑       │ controller: proxy/account/     │ Nova 4个配置段 vs Swift 3层
    │                              │   container/object服务          │
    │                              │ compute: storage节点            │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 5  │ 🆕 Cinder 计时部署（加入轮转）  │ Swift 分段存放 + iaas脚本         │ Cinder三段式 vs Swift三段式
    │ 第一次无笔记，计时基线          │ 创建 examcontainer               │ 块存储 vs 对象存储设计差异
    │                              │ 上传镜像每段10M                  │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 6  │ 🔵 Keystone 排错练习（深复习）  │ ISCSI target 配置（compute端）    │ 认证机制回顾
    │ 模拟3个故障:                   │ 安装 targetcli                  │ Keystone token流程再走一遍
    │ ① my.cnf路径写错              │ 创建 target iqn + LUN           │
    │ ② endpoint URL不对            │ 设置 ACL（仅controller可访问）      │
    │ ③ memcached没启动             │ 关闭密码验证                     │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 7  │ 🟡 联调复习（深复习·2h）        │ ISCSI 客户端连接（controller端）   │ 模块一进度评估
    │ Keystone→Glance→Neutron→     │ iscsiadm 发现→登录→挂载→格式化     │ 本周踩坑汇总
    │ Nova→Cinder 全链路部署         │ 验证读写                        │ 下周 1B 计划调整
    │ 计时目标: ≤75min               │                               │
```

> ⚠️ **Day 5: Cinder 正式进入复习池。** 从今天起，复习池 = Keystone + Glance + Neutron + Nova + Cinder（5个组件）。
> ⚠️ **Day 7 联调复习占 2h，当天主攻段缩短为 60min。** 每两周一次联调，性价比很高。
>
> 📅 **1A 日历映射：** 8/1(六) Day1 → 8/2(日)休息 → 8/3(一) Day2 → 8/4(二) Day3 → 8/5(三) Day4 → 8/6(四) Day5 → 8/7(五) Day6 → 8/8(六·上午) Day7 → 8/9(日)休息+微检。周六只练上午，下午+周日硬休息。

### 2.3 1B 界面编排（7天）

**新内容：** Dashboard → Keystone 多租户 → Heat → Cgroup  
**日历：** 8/10(一) → 8/17(一)，中间 8/16(日) 休息

> 📅 **1B 日历映射：** 8/10(一) Day1 → 8/11(二) Day2 → 8/12(三) Day3 → 8/13(四) Day4 → 8/14(五) Day5 → 8/15(六·上午) Day6 → 8/16(日)休息 → 8/17(一) Day7

**Dashboard 赛题考点（Day1-2）：**

| 考点     | 配置                                                       | 说明                                                               |
| :------- | :--------------------------------------------------------- | :----------------------------------------------------------------- |
| 部署     | `iaas-install-horizon.sh`                                  | 脚本一键，依赖 Keystone 已就绪                                     |
| 会话存储 | `SESSION_ENGINE = 'django.contrib.sessions.backends.file'` | 改 `/etc/openstack-dashboard/local_settings`，替换默认的 memcached |
| 标题定制 | `SITE_BRANDING = "私有云云计算基础架构平台"`               | 同文件，改登录页+浏览器标签标题                                    |
| 重启     | `systemctl restart httpd.service`                          | openEuler 上 Apache 服务名是 `httpd`                               |
| 验证     | 浏览器访问 `http://controller/dashboard`                   | 标题变 + 能登录 = 成功                                             |

> 💡 Horizon 三段式特殊：有 `keystone_authtoken` 段但**没有 `[database]` 段**——它不直接连数据库，数据通过 Django 文件存储。

```
复习池：Keystone / Glance / Neutron / Nova / Cinder / Swift / ISCSI（7个组件）
轮转周期：3天 → 每个组件每3-4天复习一次

Day │ 热身段(15-20min)              │ 主攻段(90-120min)              │ 收尾段
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 计时 Keystone（≤20min·加速）   │ Dashboard 部署                  │ Dashboard 三段式对比
    │ 目标: 越练越快                 │ iaas-install-horizon.sh         │ 它也有 keystone_authtoken!
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时 Glance                   │ Dashboard 定制                   │ Django配置 vs 其他组件
    │                              │ Django文件存储（SESSION_ENGINE）  │ 为什么Horizon不需要[database]
    │                              │ 登录页标题改为"私有云云计算基础架构平台"│
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 计时 Cinder                   │ 🆕 Keystone 多租户管理（上）       │ 域-项目-组-用户层级关系
    │ 检查 LVM 后端状态              │ 创建域 210Demo                   │ openstack CLI 速记
    │                              │ 创建项目 Engineering/Production   │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 🆕 Swift 计时部署（加入轮转）   │ 🆕 Keystone 多租户管理（下）       │ 角色绑定速记卡
    │ 检查 account/container/obj    │ 创建组 Devops + 4个用户           │ Robert/George/William/John
    │                              │ 绑定角色(member/admin) + 设邮箱    │ 各自角色+邮箱
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 5  │ 计时 Neutron（网桥再检查）      │ Heat 部署 + YAML模板编写          │ Heat vs 手搓对比
    │                              │ 编写 create_container.yaml       │ 声明式 vs 命令式
    │                              │ 一键创建 heat-swift 容器           │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 6  │ 🔵 Nova 排错练习（深复习）      │ Cgroup CPU 限制 + Heat 联调       │ 本周踩坑汇总
    │ 模拟: 调度器不工作/noVNC不通    │ Cgroup限制 → Heat创建资源 → 验证    │
    │                              │ Cgroup cpu_limit ≤20% 单核        │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 7  │ 🟡 联调复习（2h）              │ Dashboard+Keystone多租户+Heat     │ 1B 完成评估
    │ Keystone→ISCSI 全链路          │ +Cgroup 四合一综合练习             │ 下周 1C 计划
    │ 计时目标: ≤90min              │                               │
```

### 2.4 1C 调优参数（7天）

**新内容：** 五大组件调优参数（MariaDB/Memcached/Glance/Nova/Keystone）+ Nova 缓存调度器

> 💡 **核心策略：** 调优参数是"配置文件里改一两个值"，不需要像新组件那样从头部署。每天主攻段可以练 2-3 个参数，多出来的时间用于复习池。

```
复习池：Keystone / Glance / Neutron / Nova / Cinder / Swift / ISCSI / Dashboard / Heat（9项）
复习池太大 → 拆分策略:
  · 核心组件(7个OS组件): 轮转，每3天1次
  · 工具类(Dashboard/Heat/Cgroup): 每7天1次（维持池）

Day │ 热身段(15-20min)              │ 主攻段(90-120min)              │ 收尾段
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 计时 Keystone                 │ MariaDB 调优                    │ 调优参数速记卡
    │                              │ · max_allowed_packet=64M        │ 每学一个参数写一行卡片
    │                              │ · innodb_buffer_pool_size=4G     │
    │                              │ · innodb_log_buffer_size=64MB    │
    │                              │ · innodb_log_file_size=256MB     │
    │                              │   + innodb_log_files_in_group=2  │
    │                              │ · lower_case_table_names=1       │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时 Glance                   │ Memcached 调优                   │ 调优参数速记卡
    │                              │ · -m 512 (内存512MB)              │
    │                              │ · -c 2048 (最大连接数)             │
    │                              │ · -h md5 (hash算法)               │
    │                              │ + Keystone调优: 启用memcached缓存  │
    │                              │   [cache] backend=dogpile.cache   │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 计时 Neutron                  │ Glance 调优                      │ 调优参数速记卡
    │                              │ · 镜像缓存: cache_dir + 50GB     │
    │                              │   + 12h自动清理                   │
    │                              │ · API调优: socket_timeout=180     │
    │                              │   + KeepAlive On                  │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 计时 Nova                     │ Nova 调优（3项）                  │ 调优参数速记卡
    │                              │ · 🆕 缓存调度器: driver=caching     │
    │                              │   scheduler + 缓存主机信息(不重启)   │
    │                              │ · 并发限制: max_concurrent=3      │
    │                              │ · 白名单优化: 缩短命令加载开销       │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 5  │ 计时 Cinder                   │ 调优参数回顾 + 全组件联调验证        │ 十大调优参数速记卡
    │                              │ 把1C学的参数全部验证一遍             │ 整理成一张表
    │                              │ 改参数→重启服务→验证生效             │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 6  │ 🆕 Dashboard 计时部署（加入）   │ 调优参数"破坏-修复"练习             │ 排错笔记
    │ 含标题定制                     │ 请人/自己随机改错一个参数           │
    │                              │ 通过日志定位并修复                  │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 7  │ 🟡 联调复习（2h）              │ 模块一服务+调优 完整走一遍           │ 1C 完成评估
    │ Keystone→ISCSI+Dashboard+Heat │ 目标: 部署+全调优 ≤120min        │ 下周 1D 计划
    │ +各组件调优验证                │                               │
```

### 2.5 1D 监控工具（5天）

**新内容：** Prometheus+Grafana裸机 → node_exporter监控带宽 → qemu镜像压缩

```
复习池(核心OS组件): Keystone/Glance/Neutron/Nova/Cinder/Swift/ISCSI
维持池(工具类): Dashboard/Heat/Cgroup + 全部调优参数

Day │ 热身段(15-20min)              │ 主攻段(90-120min)              │ 收尾段
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 计时 Keystone                 │ Prometheus 部署（/opt/monitor/）│ 监控 vs 被监控
    │                              │ · tar解压到/opt/monitor/         │ Prometheus三段式:
    │                              │ · prometheus.yml 基础配置         │ 数据源/规则/告警
    │                              │ · systemd 服务文件编写             │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时 Nova                     │ Node Exporter 部署               │ 监控体系笔记
    │                              │ · 另一台主机装 node_exporter      │ Prometheus拉 + Exporter推
    │                              │ · 放到 /usr/local/                │
    │                              │ · Prometheus 添加 node job        │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 计时 Cinder                   │ Grafana 部署 + 数据源配置          │ 面板配置笔记
    │                              │ · 部署到 /opt/monitor/            │
    │                              │ · systemd + 端口3000              │
    │                              │ · 添加 Prometheus 数据源           │
    │                              │ · 导入 JSON 面板，监控网络带宽       │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 🆕 Prometheus+Grafana 加入    │ qemu 镜像压缩                     │ 本周踩坑
    │ 维持池: 检查服务状态+面板正常    │ · qemu-img convert -c 压缩       │
    │                              │ · CentOS7.5-compress.qcow2        │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 5  │ 🟡 联调复习（2h）              │ 模块一所有服务+监控+镜像            │ 1D 完成评估
    │ 全链路 + Prometheus面板验证    │ 模拟赛题环境完整走一遍              │ 下周 1E Python开发
```

### 2.6 1E Python 开发（7天）

**新内容：** MariaDB 操作类 + OpenStack 用户管理 CLI

> 🎯 **关键转折：** 这是模块一的 Python 开发——但 Python 基础语法你要提前准备好。如果到这天还不会 Python 基础，先花 2 天补基础再进入 1E。

```
复习池(核心OS组件): 每3天轮转
维持池: Dashboard/Heat/Cgroup/Prometheus+Grafana/调优参数 → 每7天
新增 Python 维持项: 每天收尾段跑一遍前一天写的脚本

Day │ 热身段(15-20min)              │ 主攻段(90-120min)              │ 收尾段
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 计时 Swift                    │ Python 环境准备                  │ PyMySQL 连接模板
    │                              │ · Python 3.7.3 安装（赛题版本）    │ connect → cursor → execute
    │                              │ · pip install pymysql fastapi     │
    │                              │ · argparse 基础（命令行参数解析）    │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时 ISCSI（check ACL）       │ DM_Manager 类编写（上）            │ CRUD 模板速记
    │                              │ · __init__(db_name): 建库连库      │ CREATE TABLE / INSERT /
    │                              │ · create_table(): member表        │ UPDATE / SELECT / DELETE
    │                              │   (id自增, name, level, place)     │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 计时 Keystone                 │ DM_Manager 类编写（下）            │ 全部方法速记卡
    │                              │ · insert_table_data(**kwargs)     │
    │                              │ · update_table_data(**kwargs)     │
    │                              │ · run_table_raw_sql(raw_sql)      │
    │                              │ · delete_table()                  │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 🔵 排错练习: Cinder            │ FastAPI + argparse CLI 开发（上） │ CLI 框架速记
    │ 模拟LVM后端挂载失败             │ · FastAPI app 初始化（端口9046）   │ argparse → 路由 → 函数
    │                              │ · argparse 子命令: list/show/     │
    │                              │   create/delete                   │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 5  │ 计时 Dashboard（含标题）       │ FastAPI + argparse CLI 开发（下） │ 完整CLI速记
    │                              │ · list: 表格输出(ID/Name)         │
    │                              │ · show: Field-Value表格           │
    │                              │ · create: --name --password       │
    │                              │ · delete: 按ID或Name删除          │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 6  │ 🔵 排错练习: Neutron           │ Python 开发综合练习                │ 1E 踩坑笔记
    │ 模拟网桥名称错误                │ · 从头写一遍 DM_Manager + CLI     │
    │                              │ · 不翻代码，只翻 API 文档           │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 7  │ 🟡 联调复习（2h）              │ 模块一全链路 + Python开发验证       │ 1E 完成评估
    │ → 然后跑 Python 脚本验证        │ 部署→调优→监控→跑开发脚本          │ 下周 1F 整合拉练
```

### 2.7 1F 整合拉练（4天）

**目标：** 把模块一所有内容压缩成比赛节奏。

```
Day │ 热身段                         │ 主攻段                         │ 收尾段
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 1  │ 联调复习                        │ Shell一键脚本编写                │ 脚本框架速记
    │ 快速全链路（目标≤60min）          │ Keystone→ISCSI 全流程封装         │ 变量分离: 密码/IP/网卡
    │                              │ 变量文件 + 主脚本分离               │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 2  │ 计时部署（随机抽1个组件）         │ iaas-yoga 全流程计时练习           │ 计时基线
    │                              │ 从零开始: 配yum→基础服务→8个组件     │ 记录每段耗时
    │                              │ 目标: 全流程 ≤60min               │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 3  │ 调优参数速记卡自测               │ 模块一模拟考                      │ 模拟考复盘
    │ 10个调优参数默写                 │ 完整赛题条件: 部署+调优+监控+       │ 哪里慢了/错了
    │                              │   Python开发+镜像压缩             │
    │                              │ 限时 2.5h                        │
────┼──────────────────────────────┼───────────────────────────────┼──────────────────────────
 4  │ 模拟考错题重练                   │ Shell脚本调试 + 排错笔记整理        │ 🎉 模块一完成
    │ 针对昨天出错的点专项补练           │ 最终版一键脚本保存                 │ 转入模块二
    │                              │                               │ 复习策略切换: 🔵→🟢
```

---

## 三、模块二：Kubernetes 容器云（6-8周）

### 3.1 赛题考点与子阶段

| 子阶段            | 新学内容                                                                           | 赛题对应     | 预计天数 |
| :---------------- | :--------------------------------------------------------------------------------- | :----------- | :------- |
| **2A K8s底座**    | K8s集群(Master+Node)+Harbor镜像仓库+Istio+kubeVirt                                 | 2.1.1-2.1.4  | 10天     |
| **2B 容器化监控** | Dockerfile四件套(node_exporter/alertmanager/grafana/prometheus)+docker-compose编排 | 2.2.1-2.2.5  | 8天      |
| **2C CI/CD**      | GitLab+Runner+Agent+.gitlab-ci.yml+Harbor推送+K8s部署                              | 2.2.6-2.2.9  | 10天     |
| **2D 调优+开发**  | Etcd配额+K8s控制器调优+Docker API+K8s监控API(FastAPI)                              | 2.2.10-2.3.2 | 7天      |
| **2E 模块二拉练** | 全流程计时+Shell脚本+K8s排错专项                                                   | —            | 4天      |

### 3.2 复习池管理（模块二期间）

```
🔵 活跃复习池（每3天轮转）: K8s底座+新学容器化组件
🟢 维持池-模块一（每7天1次，每次1h）:
   · 单组件计时部署（随机抽2个OpenStack组件）
   · 调优参数速记卡默写
   · Python开发脚本重跑
⚪ 长期池: 暂无（模块一还没到3个月）
```

### 3.3 模块二期间的"模块一维持"示例

每周选一天（建议周三或周四），热身段延长到 40min，做模块一回顾：

```
每周模块一维持日:
  热身段(40min): 随机抽2个OpenStack组件计时部署 + 5个调优参数默写
  主攻段(80min):  继续模块二新内容（时间略短但不断档）
  收尾段(15min):  对比: OpenStack调优 vs K8s调优（都是改配置文件+重启）
```

### 3.4 2A-2E 每日轮转简表

篇幅原因，只列出每阶段的核心轮转逻辑：

**2A K8s底座（10天）:**

- 热身段: 模块二新组件按学习顺序轮转（K8s→Harbor→Istio→kubeVirt，每3天一轮）
- 每周三: 模块一维持（抽2个OS组件+调优参数）
- Day 10: 联调复习（K8s+Harbor+Istio+kubeVirt 全链路）

**2B 容器化监控（8天）:**

- 热身段轮转: K8s部署 + 模块二已学组件
- 主攻段: Dockerfile四件套（每个组件2天: 1天写Dockerfile，1天compose编排）
- 特别注意: 赛题用特定版本号（node_exporter-0.18.1、alertmanager-0.19.0、grafana-6.4.1、prometheus-2.13.0）

**2C CI/CD（10天）:**

- 热身段轮转: K8s + Dockerfile四件套
- 主攻段: GitLab部署→Runner→Agent→.gitlab-ci.yml→全链路验证
- 特别注意: GitLab Agent是2026新考点，连接K8s集群的方式跟Runner不同

**2D 调优+开发（7天）:**

- 热身段轮转: 模块二所有已学组件
- 主攻段: Etcd配额+K8s控制器调优+Docker API Python+K8s监控API
- Python开发: Docker SDK(Python) 创建nginx容器 + FastAPI 资源监控API

**2E 模块二拉练（4天）:**

- 同模块一1F结构: Shell脚本→全流程计时→模拟考→错题重练

---

## 四、模块三：边缘计算（4-5周）

### 4.1 赛题考点

| 子阶段            | 新学内容                                                     | 赛题对应    | 预计天数 |
| :---------------- | :----------------------------------------------------------- | :---------- | :------- |
| **3A 平台部署**   | KubeEdge 1.11(cloudcore+edgecore)+MQTT编译+两台edge节点      | 3.1.1-3.1.2 | 7天      |
| **3B 边缘应用**   | DeviceModel(counter-model)+Device(counter)+云端控制器+计数器 | 3.1.3       | 5天      |
| **3C 云服务开发** | FastAPI AI人脸识别(OpenCV,端口7047)+边缘设备CRUD(端口8070)   | 3.2.1-3.2.2 | 7天      |
| **3D 模块三拉练** | 全流程+模拟考                                                | —           | 3天      |

### 4.2 复习池管理（模块三期间）

```
🔵 活跃复习池: KubeEdge + MQTT + DeviceModel/Device + FastAPI开发
🟢 维持池-模块二（每7天1次，每次1h）:
   · K8s+Harbor+Istio+kubeVirt 部署抽查
   · Dockerfile四件套重写
   · GitLab CI/CD 流水线重跑
🟢 维持池-模块一（每14天1次，每次45min）:
   · iaas-yoga 全流程计时（目标≤45min）
   · 调优参数速记卡全部默写
   · Python开发脚本抽查
```

---

## 五、模块四：大语言模型应用（3-4周）

### 5.1 赛题考点

| 子阶段            | 新学内容                                                            | 赛题对应    | 预计天数 |
| :---------------- | :------------------------------------------------------------------ | :---------- | :------- |
| **4A 平台部署**   | Dify 1.9.1(超时+文件限制)+Ollama容器(离线模型导入)                  | 4.1.1-4.1.2 | 5天      |
| **4B 聊天机器人** | Streamlit+Ollama(多轮对话+文件解析docx/txt/pdf+多模型切换+流式输出) | 4.2.1       | 7天      |
| **4C 模块四拉练** | 全流程+模拟考                                                       | —           | 3天      |

### 5.2 复习池管理（模块四期间）

```
🔵 活跃复习池: Dify + Ollama + Streamlit 开发
🟢 维持池-模块三（每7天1次）:
   · KubeEdge cloudcore+edgecore 部署
   · DeviceModel/Device YAML 重写
   · FastAPI 人脸识别 + 设备CRUD 脚本重跑
🟢 维持池-模块二（每14天1次）:
   · K8s集群+Dockerfile+docker-compose 抽查
⚪ 长期池-模块一（每14天1次，45min）:
   · OpenStack 全链路计时 + 调优抽查
```

---

## 六、真题拉练（最后4周）

### 6.1 拉练节奏

```
Week 1: 模块一+模块二 混合模拟考
Week 2: 模块三+模块四 混合模拟考
Week 3: 四模块全真模拟（连续2天，每天3.5h）
Week 4: 错题专攻 + 排错笔记过一遍 + 状态调整
```

### 6.2 最后4周的复习池

此时所有模块都进入维持/长期池：

```
每周一: 模块一（OpenStack全链路 45min）
每周二: 模块二（K8s+CI/CD 45min）
每周三: 模块三（KubeEdge+FastAPI 45min）
每周四: 模块四（Dify+Streamlit 45min）
每周五: 弱点专项（本周模拟考暴露的问题）
每周六下午+周日: 硬休息
```

---

## 七、执行纪律

### 7.1 每日铁律

1. **热身段不可跳过。** 哪怕只做 10min 计时部署，也比不做强一万倍——这是防止遗忘的最后防线。
2. **收尾段必须写一句话。** 格式: `> ⚠️ 今天踩坑：___ → 怎么解决的___。明天热身选___。`
3. **遇到卡壳 30min 以上：停。** 去热身段重新部署一个老组件找回信心，第二天再攻。

### 7.2 每周铁律

1. **周六下午+周日全天硬休息。** 不可挪用。休息日学了的代价是下周效率减半。
2. **周日 15min 微检。** 三个问题: ①本周产出了什么？②和子阶段目标偏离了多少？③下周最该砍掉什么？

### 7.3 模块切换铁律

1. **模块完成后必须做一次全真模拟考**（限时、不翻笔记、模拟赛题条件）
2. **模拟考暴露的问题优先于新模块计划**——先补漏再前进
3. **切换前把旧模块的"维持复习脚本"写好**——一个 `review-module-1.sh` 一键跑完，降低维持成本

---

## 八、复习工具清单

为了降低维持成本，每个模块学完后产出一个复习脚本：

| 模块   | 复习脚本         | 内容                                                   |
| :----- | :--------------- | :----------------------------------------------------- |
| 模块一 | `review-os.sh`   | 随机抽2个组件计时部署 + 全部调优参数检查               |
| 模块二 | `review-k8s.sh`  | K8s集群状态检查 + 随机Dockerfile重写 + CI/CD流水线验证 |
| 模块三 | `review-edge.sh` | KubeEdge状态检查 + Device CRUD重跑 + AI识别接口测试    |
| 模块四 | `review-llm.sh`  | Dify+Ollama状态检查 + Streamlit多轮对话测试            |

---

## 九、后续细化说明

本文档已完成模块一（1A-1F）的逐日轮转表，以及模块二至模块四的框架设计。

模块二至模块四的逐日轮转表将在**进入对应模块前 3 天**细化——届时会根据实际进度和模块一的模拟考结果做校准，避免提前过度设计。

---

> 📅 **制定:** 2026-07-26 · 📅 **下次回顾:** 1A 存储收尾完成后（约 7 天）· 📅 **模块一完成后全面复盘**
